#include "main.h"
/**
 * main - Entry point
 * @argc: argument counter
 * @argv: argument vector
 * Return: return the neumber ofargument passed on it
*/
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%i\n", argv[argc]);
	return (0);
}
